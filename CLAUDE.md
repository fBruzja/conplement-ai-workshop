# Incident Tracker

This project keeps a **single source of truth** for agent context in `AGENTS.md`, imported below.
`AGENTS.md` is read natively by GitHub Copilot CLI, Codex and Cursor; the import makes Claude Code
load the identical content. Edit `AGENTS.md`, never this file.

@AGENTS.md
